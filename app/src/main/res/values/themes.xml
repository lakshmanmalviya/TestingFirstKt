<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Myapp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
<!--  the to most color where battery shows-->
        <!-- Customize your theme here. -->
    </style>
    <style name="cornerTopLeft">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">4dp</item> <!-- Adjust the corner radius as needed -->
        <item name="cornerSizeBottomLeft">4dp</item> <!-- Adjust the corner radius as needed -->
    </style>
    <style name="playlistImgStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">8dp</item>
        <item name="cornerSizeTopRight">8dp</item>
        <item name="cornerSizeBottomLeft">8dp</item>
        <item name="cornerSizeBottomRight">8dp</item> <!-- Adjust the corner radius as needed -->
    </style>

</resources>